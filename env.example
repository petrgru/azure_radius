# Azure AD Configuration
AZURE_TENANT_ID=xxx
AZURE_CLIENT_ID=xxx
AZURE_CLIENT_SECRET=xxx
RADIUS_SECRET=testing123
PORT=1812

# Database Configuration (if using MySQL)
DB_HOST=radius-mysql-prod
DB_PORT=3306
DB_NAME=radius_users
DB_USER=radiususer
DB_PASSWORD=your-db-password

# Domain Configuration
ALLOWED_DOMAINS=sspu-opava.cz

# RADIUS Server ID (for multi-server support)
RADIUS_SERVER_ID=radius1

# Debug Configuration
DEBUG=error,warn
NODE_ENV=production
